# Synthetic fixtures

Nine constructed traces used in unit tests, and as a check that the rules generalize past the 17-trace development subset. Not a held-out set.

Six dirty: instruction skip, missing argument, evidence contradiction, unsupported success, identical repeat with phantom failure, ignored feedback.

Three clean look-alikes: justified 429 retry, required confirmation, multi-id search.
